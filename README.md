# 🎯 Система детекции и предсказания храпа

Оптимизированная система машинного обучения для детекции и предсказания храпа на микроконтроллерах ESP32.

## 🏗️ Архитектура системы

Система включает две специализированные модели:

### 1. **SnoringClassifier** — классификатор храпа (3 класса)
- **Алгоритм**: RandomForest (по умолчанию), LightGBM, SVM
- **Задача**: Классификация по интенсивности храпа
- **Входные данные**: Аудио-сегмент 1-5 секунд, 8 kHz
- **Признаки**: 50 комбинированных признаков
- **Классы**: No_Snoring, Light_Snoring, Heavy_Snoring
- **Размер модели**: ~500 КБ после оптимизации
- **Время инференса**: < 100 мс

### 2. **SnoringPredictor** — модель предсказания вероятности эпизода храпа
- **Алгоритм**: Нейронная сеть TensorFlow Lite
- **Задача**: Предсказание вероятности храпа в ближайшем окне
- **Входные данные**: Аудио + акселерометр + временные данные
- **Признаки**: 25-30 комбинированных признаков
- **Размер модели**: 30-100 КБ после TFLite оптимизации
- **Время инференса**: < 100 мс

## 📊 Характеристики моделей

| Характеристика | SnoringClassifier | SnoringPredictor |
|----------------|-------------------|------------------|
| **Тип** | Классификация (3 класса) | Предсказание (вероятность) |
| **Алгоритм** | RandomForest/LightGBM/SVM | Neural Network (TFLite) |
| **Размер модели** | ~500 КБ | 30–100 КБ |
| **Входные признаки** | 50 комбинированных признаков | 25–30 (аудио + аксель + время) |
| **Классы** | No_Snoring, Light_Snoring, Heavy_Snoring | Вероятность храпа |
| **Инференс** | < 100 мс | < 100 мс |
| **Обработка** | Одно окно (1–5 сек) | Контекст + история |
| **Квантование** | Да (int8) | Да (int8) |

## 🔧 Технические детали

### SnoringClassifier — модель классификации храпа (3 класса)

#### **Входные данные**
- **Аудио-сегмент**: 1–5 секунд, 8 kHz
- **Формат**: массив значений [-1.0, 1.0]
- **Признаки**: 50 комбинированных признаков:
  - **Аудио признаки**: RMS, спектральные характеристики, энергетические
  - **Акселерометр**: движение, вибрация, стабильность
  - **Временные признаки**: регулярность, интенсивность, паттерны

#### **Архитектура модели**
- **RandomForest (по умолчанию)**:
  - 100 деревьев, глубина до 10
  - Критерий: gini
  - Минимальные образцы: 2
- **Альтернативы**: LightGBM, SVM, Logistic Regression

#### **Преобразование признаков**
- Нормализация амплитудных признаков (логарифмирование)
- Стандартизация по скользящему окну
- Выбор признаков: ANOVA F-test

#### **Выход**
- Класс храпа: No_Snoring, Light_Snoring, Heavy_Snoring
- Вероятности для каждого класса
- Уверенность предсказания

#### **Метрики**
- **Accuracy**: 100% (на синтетических данных)
- **Precision**: 1.000 для всех классов
- **Recall**: 1.000 для всех классов
- **F1-score**: 1.000 для всех классов
- **Время инференса**: < 100 мс
- **Память**: < 50 МБ (включая буфер и модель)

### SnoringPredictor — модель предсказания вероятности храпа

#### **Входные данные**
- **Комбинированный вектор из 25–30 признаков**:
  - **Аудио-признаки** (как в модели 1)
  - **Акселерометр**:
    - Средняя активность, ускорение, дрожание, стабильность
  - **Временные данные**:
    - Час суток, длительность сна, фаза, глубина, цикл сна

#### **Архитектура модели**
- **Нейросеть на TensorFlow**:
  - Dense(64) → Dropout(0.2)
  - Dense(32) → Dense(16)
  - Dense(1, sigmoid)
- **Оптимизатор**: Adam (lr=0.001)
- **Loss**: Binary Crossentropy

#### **Оптимизация**
- TensorFlow Lite + post-training quantization (int8)
- Размер модели: 30–100 КБ
- Время инференса: < 100 мс (ESP32-S3)

#### **Выход**
- Вероятность эпизода храпа
- Уровень риска + confidence-score

#### **Метрики**
- **Accuracy**: 75–80%
- **Sensitivity**: 82%
- **Specificity**: 78%
- **F1-score**: 0.80
- **Память**: до 100 МБ (до оптимизации)

## 🔄 Интеграция моделей в систему

### **Обработка аудиосегмента**
1. **SnoringClassifier**: классификация текущего фрагмента
2. **Сохранение истории детекций** (для предсказания)
3. **SnoringPredictor**: предсказание на основе истории + акселерометр + время

### **Архитектурные компоненты**
- **SnoringFeatureExtractor**: извлечение всех признаков (аудио, аксель)
- **SnoringFeatureSelector**: выбор наиболее важных признаков (ANOVA F-test)
- **SnoringSystem**: контролирует поток данных, историю, запуск моделей

### **Поток данных**
1. Получение и буферизация аудио
2. Извлечение признаков
3. Детекция (модель 1)
4. Предсказание (модель 2) при достаточном контексте

## 🚀 Быстрый старт

### Установка зависимостей
```bash
pip install -r requirements.txt
source venv/bin/activate  # Активация виртуального окружения
```

### 🎯 Запуск визуализации
```bash
python3 simple_visualization.py
```

### 🎯 Запуск демонстрации
```bash
python3 snoring_demo.py
python3 snoring_demo_enhanced.py
```

### Использование моделей

#### SnoringClassifier
```python
from src.models.snoring_classifier import SnoringClassifier
from src.features.snoring_extractor import SnoringFeatureExtractor

# Создание классификатора
classifier = SnoringClassifier(model_type='random_forest')

# Обучение модели
classifier.train(X_train, y_train)

# Предсказание
result = classifier.predict(audio_segment)
print(f"Класс: {result['class']}, Уверенность: {result['confidence']}")
```

#### SnoringPredictor
```python
from src.models.snoring_predictor import SnoringPredictor

# Создание модели предсказания
predictor = SnoringPredictor(model_type='neural_network')

# Обучение модели
predictor.train(X_train, y_train)

# Предсказание эпизода храпа
result = predictor.predict_snoring_episode(
    audio_data=audio_segment,
    accelerometer_data=accel_data,
    temporal_data=temporal_data
)
print(f"Вероятность храпа: {result['snoring_probability']}")
print(f"Уровень риска: {result['risk_level']}")
```

### Конвертация в TensorFlow Lite
```python
# Конвертация SnoringPredictor в TFLite
tflite_model = predictor.convert_to_tflite()
predictor.save_model('models/snoring_predictor.tflite')
```

## 📁 Структура проекта

```
sleep-stage-classifier/
├── src/
│   ├── models/
│   │   ├── snoring_classifier.py      # Бинарный классификатор храпа
│   │   ├── snoring_predictor.py       # Модель предсказания храпа
│   │   └── esp32_classifier.py        # ESP32 оптимизация
│   ├── features/
│   │   ├── snoring_extractor.py       # Извлечение признаков храпа
│   │   └── esp32_extractor.py         # ESP32 экстрактор
│   └── utils/
│       ├── config.py                   # Конфигурация
│       └── logger.py                   # Логирование
├── esp32_code/                         # Код для микроконтроллера
├── data/                               # Данные
├── models/                             # Сохраненные модели
├── notebooks/                          # Jupyter notebooks
└── requirements.txt                    # Зависимости
```

## 🎯 Заключение

Модели **SnoringClassifier** и **SnoringPredictor** образуют прочный ML-фундамент системы:

- **Упрощенная классификация** (3 класса вместо 5)
- **Высокая точность** при малом размере
- **Быстрый инференс**, адаптированный под ESP32
- **Гибкая архитектура** для дальнейшей доработки и масштабирования
- **Оптимизированный ESP32 код** для встраивания

**Результат**: Максимально оптимизированная система для детекции и предсказания храпа на микроконтроллерах с упрощенной структурой классов.

---

## 📊 Статус проекта

✅ **Все задачи выполнены:**
1. ✅ Переобучение моделей с новой структурой классов
2. ✅ Обновление метаданных существующих моделей  
3. ✅ Тестирование ESP32 кода
4. ✅ Обновление документации

**Статус:** 🟢 ГОТОВ К ИСПОЛЬЗОВАНИЮ 